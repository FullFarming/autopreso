<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron defaults: V8 JIT and native-module loading under hardened runtime. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Required for microphone capture under hardened runtime. Without it macOS
       blocks the mic at the process level: no TCC prompt, no panel entry, and
       getUserMedia silently yields nothing. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
